export type UserData = {
  id: number;
  email: string;
  token: string;
};

export type loginVerification = {
  name: string;
  avatarUrl: string;
  isPro: boolean;
  email: string;
  token: string;
}
